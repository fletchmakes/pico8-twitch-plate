var config = {
	channel: "fletchmakes",		//set your channel name between the quotes, ie: "eateren"
	waitTimeUser: 10,		//set the cooldown time per user in seconds
	waitTimeGlobal: 0,		//set the global cooldown time in seconds
	command: "confetti",	//the command to make it rain
};